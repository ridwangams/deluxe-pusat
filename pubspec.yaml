name: deluxe_teriosh
description: Deluxe Teriosh Database App
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.0
  flutter_local_notifications: ^16.3.0
  http: ^1.1.0

flutter:
  uses-material-design: true
